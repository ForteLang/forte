// Zero One — transitions parts.
import { Downlifter } from "../../instruments/fx.forte"

block Exhale { track D { instrument Downlifter() volume 0.4 pan -0.1 play beat`x--- ---- ---- ----` at bars(1..1) } }
