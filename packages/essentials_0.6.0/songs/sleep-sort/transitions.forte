// Sleep Sort — transitions parts.
import { Downlifter } from "../../instruments/fx.forte"

block Sigh { track D { instrument Downlifter() volume 0.32 pan -0.08 play beat`x--- ---- ---- ----` at bars(1..1) } }
