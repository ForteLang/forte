[package]
name = "dawcore"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Real-time audio engine and DSP for the Bitwig-style DAW. No GUI or hardware dependencies."

[dependencies]
ringbuf = "0.4"

[dev-dependencies]
hound = "3.5"
